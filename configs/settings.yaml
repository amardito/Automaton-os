app:
  name: "Automaton OS"
  mode: "dev" # dev | autonomous

models:
  default:
    provider: ollama
    model: "qwen3.5:9b"

  administrator:
    provider: ollama
    model: "qwen3.5:9b"

  research:
    provider: ollama
    model: "qwen3.5:9b"

  analyst:
    provider: ollama
    model: "qwen3.5:9b"

  writer:
    provider: ollama
    model: "qwen3.5:9b"

providers:
  ollama:
    base_url: "http://localhost:11434"

  # Any OpenAI-compatible API.
  openai_compatible:
    base_url: "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/ai/v1"
    api_key: "${CLOUDFLARE_API_TOKEN}"

obsidian:
  enabled: true
  vault_path: "${OBSIDIAN_VAULT_PATH}"
  automaton_folder: "Automaton"

memory:
  sqlite_path: "memory/automaton_os.db"

research:
  max_results: 5
