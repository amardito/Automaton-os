scheduled_missions:
  - name: "Research local AI company architecture"
    enabled: true
    interval_seconds: 60
    mission: "Research one practical improvement for building Automaton-OS as a local AI company using CrewAI, Ollama, Cloudflare, and Obsidian."

  - name: "Research safe coding agents"
    enabled: false
    interval_seconds: 600
    mission: "Research safe patch-based code editing for local AI agents and summarize practical implementation ideas."